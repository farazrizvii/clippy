import React from 'react'
import ReactDOM from 'react-dom/client'
import ClippyApp from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <ClippyApp />
  </React.StrictMode>
)
